#include <stdio.h>
#include "../../include/escal.h"
// Eliminação de Gauss(Escalonamento).

// A função deverá receber uma matriz de tamanho definido:

/*Exemplo:

    int matriz[3][3] = {
        {1, 2, 3},
        {4, 5, 6},
        {7, 8, 9}
    }

    escal(matriz[3][3]);
*/
int escal(double a[tamanho][tamanho]){
    scanf("", );


    return 0;
}